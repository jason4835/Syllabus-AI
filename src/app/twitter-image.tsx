/**
 * X/Twitter uses the same 1200x630 card as Open Graph — one image to keep in
 * sync, re-exported so Next generates the `/twitter-image` route too.
 */
export { default, alt, size, contentType } from "./opengraph-image";
